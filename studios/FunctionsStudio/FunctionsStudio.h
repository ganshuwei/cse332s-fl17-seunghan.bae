#ifndef FUNCTIONS_STUDIO_H
#define FUNCTIONS_STUDIO_H

// unsigned int factorial(unsigned int num);
void factorial(unsigned int num, unsigned int * ans);

unsigned int power(unsigned int x = 3, unsigned int y = 2);

void arrayFunc(size_t length, unsigned int * ptr);

#endif /* FUNCTIONS_STUDIO_H */
